# Any config lines prefixed with this will be passed in the conf dictionary
CONFIG_PREFIX = "base"

# Basic example module - does nothing
class ModuleMain:
	def __init__(self, conn, channel, conf):
		pass
